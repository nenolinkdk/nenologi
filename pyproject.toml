[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "nenologi-core"
version = "0.1.0"
description = "Typed domain foundation for structural, semantic, and logical text analysis"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
