# AI layer

The AI layer primarily maps natural language to a structured semantic representation and generates clear explanations. Deterministic code should perform structured comparisons where practical.
