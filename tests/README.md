# Tests

- `gold_standard/` contains public, synthetic v0.1 regression cases.
- `validation/` contains standard-library validation and unit tests for the specification data.
- `comparison/`, `examples/`, and `unit/` remain placeholders for later engine tests.

See [Gold-standard guide](gold_standard/README.md) for commands and contribution rules.
